---
title: "AgenticNav：将零样本视觉语言导航作为工具调用框架"
original_title: "AgenticNav: Zero-Shot Vision-and-Language Navigation as a Tool-Calling Harness"
authors: "Yijian Li, Changze Li, Hantian Shi, Jiaying Luo, Jiyuan Cai, Ming Yang, Tong Qin"
date: 20260609
pdf: "https://arxiv.org/pdf/2606.10577v1"
tags: ["query:VLN", "query:VLN-CE", "topic:Robot Navigation", "topic:VLN", "topic:Planning / Policy"]
score: 7.5
evidence: "命中 VLN、VLN-CE；具身/机器人导航焦点明确"
selection_source: "速读区"
solved_problem: "这篇论文解决了连续环境中零样本视觉语言导航过度依赖学习式航点预测器、深度输入利用不足以及记忆上下文冗余的问题。"
tldr: "AgenticNav把VLN-CE做成一个让VLM调用动作、深度和记忆工具的轻量导航框架。"
key_contributions: ["提出面向零样本VLN-CE的AgenticNav轻量工具调用导航框架", "设计像素级动作工具和按需像素深度工具，减少对学习式航点预测器的依赖", "引入紧凑地图记忆和选择性视觉回忆，并在R2R-CE零样本方法中达到新的SOTA表现"]
motivation: "这篇论文解决了连续环境中零样本视觉语言导航过度依赖学习式航点预测器、深度输入利用不足以及记忆上下文冗余的问题。"
method: "论文提出AgenticNav，将VLM与环境之间的导航过程设计为工具调用接口：动作工具让VLM直接在RGB观测中选择目标像素并转换为可执行运动；像素深度工具按需返回关键位置的精确距离；记忆工具用紧凑地图图像总结历史轨迹，并通过回忆工具选择性访问过去视觉观测。"
result: "摘要说明在R2R-CE基准上，在相同VLM骨干下，AgenticNav取得零样本方法新的SOTA；消融实验显示动作工具优于传统航点预测器，深度工具和agentic记忆也提升导航性能；还进行了真实世界验证。"
conclusion: "核心贡献是在零样本VLN-CE中用工具调用替代传统航点预测与冗长记忆堆叠，使VLM能更直接地利用图像目标、深度距离和历史轨迹进行导航决策。"
abstract_zh: "连续环境中的零样本视觉语言导航近期因大型视觉语言模型而变得可行，但现有方法常依赖学习式航点预测器提出可导航动作，限制动作空间且未充分利用深度输入；记忆也常通过累积冗长文本或视觉历史，或检索跨回合经验来实现。本文将零样本VLN-CE重新思考为VLM与环境之间的agentic接口，提出AgenticNav，一个暴露动作、深度和记忆为可调用工具的轻量导航框架。动作工具让VLM直接在RGB观测中选择目标像素并转为运动；深度工具允许按需请求关键像素的精确距离；记忆由紧凑地图图像总结历史轨迹，并通过回忆工具选择性访问过去视觉观测。在R2R-CE上，AgenticNav在相同VLM骨干下达到零样本方法新的SOTA，真实世界验证也显示其零样本泛化优势，消融表明动作工具、深度工具和agentic记忆均有贡献。"
abstract_en: "Zero-shot vision-and-language navigation in continuous environments (VLN-CE) has recently become feasible with large vision-language models (VLMs). However, existing methods typically rely on learned waypoint predictors to propose navigable actions. This severely limits the model's action space and fails to leverage depth inputs effectively. Moreover, memory is commonly handled by accumulating long textual or visual histories with substantial irrelevant context, or by retrieving cross-episode experiences, which weakens the zero-shot setting. In this paper, we rethink zero-shot VLN-CE as an agentic interface between the VLM and the environment, and present AgenticNav, a lightweight navigation harness that exposes action, depth, and memory as callable tools. Instead of choosing from predicted waypoints, the action tool allows the VLM to directly select a target pixel in RGB observations, converting it into executable motion. Depth is exposed through an on-demand pixel-depth tool, enabling the VLM to request precise metric distances only where they matter. For memory, AgenticNav provides a compact map image summarizing the historical trajectory, paired with a recall tool that allows the VLM to selectively revisit past visual observations without overwhelming the prompt context. On the R2R-CE benchmark, AgenticNav establishes new state-of-the-art (SOTA) performance among zero-shot methods given the same VLM backbone. Real-world validation further highlights its zero-shot generalization compared to prior methods. Ablations show that our action tool design outperforms traditional waypoint predictors, and that depth tool and agentic memory further contribute to navigation performance."
---

## 解决的问题

这篇论文解决了连续环境中零样本视觉语言导航过度依赖学习式航点预测器、深度输入利用不足以及记忆上下文冗余的问题。

## TL;DR（一句话总结）

AgenticNav把VLN-CE做成一个让VLM调用动作、深度和记忆工具的轻量导航框架。

## 核心贡献

- 提出面向零样本VLN-CE的AgenticNav轻量工具调用导航框架
- 设计像素级动作工具和按需像素深度工具，减少对学习式航点预测器的依赖
- 引入紧凑地图记忆和选择性视觉回忆，并在R2R-CE零样本方法中达到新的SOTA表现

## 局限性 / 需要核验

摘要未说明，需要看正文核验。

---

## 具身导航阅读笔记

- 分区：速读区；优先级：Medium；评分：7.5/10
- 主题 lane：Robot Navigation, VLN, Planning / Policy
- 命中关键词：VLN, VLN-CE
- 推荐理由：命中 VLN、VLN-CE；具身/机器人导航焦点明确
- 核验重点：摘要未说明，需要看正文核验。

## 中文摘要

连续环境中的零样本视觉语言导航近期因大型视觉语言模型而变得可行，但现有方法常依赖学习式航点预测器提出可导航动作，限制动作空间且未充分利用深度输入；记忆也常通过累积冗长文本或视觉历史，或检索跨回合经验来实现。本文将零样本VLN-CE重新思考为VLM与环境之间的agentic接口，提出AgenticNav，一个暴露动作、深度和记忆为可调用工具的轻量导航框架。动作工具让VLM直接在RGB观测中选择目标像素并转为运动；深度工具允许按需请求关键像素的精确距离；记忆由紧凑地图图像总结历史轨迹，并通过回忆工具选择性访问过去视觉观测。在R2R-CE上，AgenticNav在相同VLM骨干下达到零样本方法新的SOTA，真实世界验证也显示其零样本泛化优势，消融表明动作工具、深度工具和agentic记忆均有贡献。

## 英文摘要原文

Zero-shot vision-and-language navigation in continuous environments (VLN-CE) has recently become feasible with large vision-language models (VLMs). However, existing methods typically rely on learned waypoint predictors to propose navigable actions. This severely limits the model's action space and fails to leverage depth inputs effectively. Moreover, memory is commonly handled by accumulating long textual or visual histories with substantial irrelevant context, or by retrieving cross-episode experiences, which weakens the zero-shot setting. In this paper, we rethink zero-shot VLN-CE as an agentic interface between the VLM and the environment, and present AgenticNav, a lightweight navigation harness that exposes action, depth, and memory as callable tools. Instead of choosing from predicted waypoints, the action tool allows the VLM to directly select a target pixel in RGB observations, converting it into executable motion. Depth is exposed through an on-demand pixel-depth tool, enabling the VLM to request precise metric distances only where they matter. For memory, AgenticNav provides a compact map image summarizing the historical trajectory, paired with a recall tool that allows the VLM to selectively revisit past visual observations without overwhelming the prompt context. On the R2R-CE benchmark, AgenticNav establishes new state-of-the-art (SOTA) performance among zero-shot methods given the same VLM backbone. Real-world validation further highlights its zero-shot generalization compared to prior methods. Ablations show that our action tool design outperforms traditional waypoint predictors, and that depth tool and agentic memory further contribute to navigation performance.
